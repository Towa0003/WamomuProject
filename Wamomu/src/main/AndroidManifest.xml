<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fh.kl.wamomu"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logotrolol"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.Login"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.Register"
            android:label="Register"
            android:screenOrientation="portrait"></activity>

        <activity
            android:name=".ui.NavigationDrawer"
            android:screenOrientation="portrait"></activity>

        <activity
        android:name=".database.database"
        android:screenOrientation="portrait"></activity>

        <activity
            android:name=".database.databaseMeals"
            android:screenOrientation="portrait"></activity>

        <activity
            android:name=".database.databaseMeasurements"
            android:screenOrientation="portrait"></activity>

        <activity
            android:name=".database.databasePushMeal"
            android:screenOrientation="portrait"></activity>

        <activity
            android:name=".database.databasePushMeasurement"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".database.databasePushUser"
            android:screenOrientation="portrait"></activity>

    </application>

</manifest>
